import { TestBed, inject } from "@angular/core/testing";

import { FirebaseWrapperService } from "./firebase-wrapper.service";

describe("FirebaseWrapperService", () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FirebaseWrapperService]
    });
  });
});
