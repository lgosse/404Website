export class Commitment {
    date: Date;
    description: [{
        head: string;
        imgUrl: string;
        body: string;
        foot: string;
    }];
    subtitle: string;
    title: string;
}
